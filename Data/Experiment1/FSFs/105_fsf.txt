            currentOpenTime > maxOpenTime && output "ALERT: Door open time (" + currentOpenTime + " s) exceeds maximum allowed (" + maxOpenTime + " s). Please close the door to save energy.", Alert triggered for excessive door open time.
            ||
            currentOpenTime <= maxOpenTime && output "Door open time (" + currentOpenTime + " s) is within safe limits.", No action required; door open time is acceptable.
